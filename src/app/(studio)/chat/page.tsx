export default function ChatPage() {
  return (
    <div className="mx-auto max-w-3xl px-6 py-10">
      <h1 className="font-display text-xl tracking-tight text-body">Chat</h1>
      <p className="prose-chat mt-2 text-muted">
        The engine lands here next — ask a question, get prose, a chart when a comparison helps, and
        an action table where every row has one button.
      </p>
    </div>
  );
}
